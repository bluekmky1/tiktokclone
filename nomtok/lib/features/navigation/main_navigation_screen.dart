import 'package:flutter/material.dart';

class MainNavigationScren extends StatelessWidget {
  const MainNavigationScren({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
